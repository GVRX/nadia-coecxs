#include make.inc


all: 
	make -C src
	@echo "done making the COECXS libraries"
	make -C tools
	@echo "done making the tools"
	make -C examples
	@echo "done making the examples"
	@echo ""
	@echo "\"make\" completed successfully."
	@echo ""
	@echo "Look at the code in the examples directory to see how to get started"
	@echo ""


#install:
#	mkdir -p $(PREFIX)/include
#	cp src/*.h $(PREFIX)/include
#	mkdir -p $(PREFIX)/lib
#	cp src/lib* $(PREFIX)/lib/
#	mkdir -p bin $(PREFIX)/bin/
#	cp tools/*.exe $(PREFIX)/bin/

clean: 
	make clean -C src
	make clean -C examples
	make clean -C tools
	make clean -C doc
	rm -f *~

clobber:
	make clean
	make clobber -C examples
	make clobber -C tools
	rm -f lib/libCOECXS.so lib/libCOECXS.a
	rm -f include/*.h

#use with caution
bare:
	make clean
	make clobber
	rm -rf examples/*.ppm tools/*.ppm
	rm -rf \#*# examples/\#*# src/\#*# tools/\#*#
	rm -rf src/Makefile example/Makefile tools/Makefile
	rm -rf config.log config.status
